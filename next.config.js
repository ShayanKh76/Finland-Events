/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['source.unsplash.com', 'api.react-finland.fi', 'www.reason-conf.com', 'typeofconf.com'],
    },
}

module.exports = nextConfig
